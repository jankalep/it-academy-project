version: '3.8'
services:
  jellyfin:
    image: jellyfin/jellyfin
    container_name: jellyfin
    restart: unless-stopped
    ports:
      - 8096
    volumes:
      - config:/config
      - cache:/cache
      - media:/media
volumes:
  config:
  cache:
  media: