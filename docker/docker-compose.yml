version: "3.8"
services:
  jellyfin:
    image: jellyfin/jellyfin
    container_name: jellyfin
    user: 1001:1001
    network_mode: "host"
    volumes:
      - config:/config
      - cache:/cache
      - media:/media
    restart: "unless-stopped"
